# Essenciais — o software funciona com estes:
customtkinter
requests
numpy
psutil
Pillow

# OPCIONAL — só a rede neural usa. Pode não existir para Python muito novo
# (o PyTorch publica com atraso). Sem ele a previsão sai pelo consenso das
# teorias validadas. Instale à parte:  pip install torch
# torch
# le os arquivos procurando nome indefinido -- o erro que
# derruba o programa sem nenhum teste perceber (ver test_lint.py)
pyflakes

# ABSORVER_PDF.py importa `pypdf` e nao estava aqui. Quem seguisse o
# 0_INSTALAR_DEPENDENCIAS.bat e depois tentasse carregar os PDFs de estudo
# levava um ImportError sem explicacao -- a dependencia existia so na cabeca
# de quem escreveu o arquivo.
pypdf
